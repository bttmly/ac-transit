{
  "name": "ac-transit",
  "version": "0.0.2",
  "description": "AC Transit API client",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "mocha --timeout 5000",
    "test-watch": "mocha --watch --timeout 5000",
    "lint": "eslint --fix"
  },
  "devDependencies": {
    "dotenv": "^5.0.1",
    "eslint": "^4.19.1",
    "expect": "^22.4.3",
    "fetch-ponyfill": "^6.0.1",
    "mocha": "^5.0.5"
  }
}
